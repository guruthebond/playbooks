- name: Create temp file on execution node
  copy:
    content: "hello from {{ inventory_hostname }}"
    dest: "/tmp/awx-output.txt"
  delegate_to: localhost
  run_once: true

- name: Fetch file to controller
  fetch:
    src: "/tmp/awx-output.txt"
    dest: "/var/lib/awx/projects/"
    flat: yes
  delegate_to: localhost
  run_once: true
